name: financial_data_warehouse
version: "1.0.0"
config-version: 2

profile: financial_data_warehouse

model-paths:
  - models
test-paths:
  - tests
macro-paths:
  - macros

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  financial_data_warehouse:
    staging:
      +schema: staging
      +materialized: view
    warehouse:
      +schema: warehouse
      +materialized: table
    marts:
      +schema: analytics
      +materialized: table
